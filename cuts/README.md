# Cuts

Committed on purpose. A Cut is the reviewable artefact of this engine: a small
JSON file holding the exact quoted string and the exact citation, approved
before any frame is rendered.

Corpora (`corpus/`) and renders (`renders/`) are regenerable and gitignored;
these are not.
